# Copy this file to .env and fill in real values.
# Never commit .env to git.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/hardware_pos?schema=public"

# Required. Use a long random string in production.
JWT_SECRET="replace-with-a-long-random-secret"

PORT=3000
CURRENCY=KES
DEFAULT_VAT_RATE=16

# For automated tests, copy .env.test.example to .env.test
# and use database hardware_pos_test only.
