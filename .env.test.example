# Copy to .env.test (gitignored). NEVER point this at hardware_pos.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/hardware_pos_test?schema=public"
JWT_SECRET="test-only-jwt-secret-not-for-production"
PORT=3001
CURRENCY=KES
DEFAULT_VAT_RATE=16
